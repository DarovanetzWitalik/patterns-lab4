# PR для завдання 3 — Observer

## Що додано
- підтримку EventListener для елементів LightHTML;
- підписку на різні події (`click`, `mouseover`);
- демонстрацію виклику кількох слухачів для однієї події.

## Як перевірити
Запустити `lab4.app.Main` і переглянути секцію `Спостерігач: події LightHTML`.
